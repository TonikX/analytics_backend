import {createStyles, Theme} from "@material-ui/core";

export default (theme: Theme) => createStyles({
    wrap: {
        display: "flex"
    },
    programInput: {
        width: '200px',
    },
    titleInput: {
        width: '400px'
    },
    marginRight: {
        marginRight: '20px'
    }
});