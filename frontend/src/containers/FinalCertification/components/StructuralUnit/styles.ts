import {createStyles} from "@material-ui/core";

export default () => createStyles({
    input: {
        width: '100%',
        marginTop: '20px',
        display: "block",
    },
    structuralUnit: {
        marginTop: '37px',
    }
});