import {createStyles} from "@mui/material";

export default () => createStyles({
    input: {
        width: '100%',
        display: 'flex',
    },
    marginRight: {
        marginRight: '20px'
    }
});
