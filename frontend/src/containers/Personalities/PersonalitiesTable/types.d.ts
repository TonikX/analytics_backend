import { PersonalityType} from '../types'
import {SortingType} from "../../../components/SortingButton/types";

export interface PersonalitiesTableProps {
  personalities: Array<PersonalityType>;
//   sortingField: string;
//   sortingMode: SortingType;
}