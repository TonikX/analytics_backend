import createStyles from "@mui/styles/createStyles";

export default () => createStyles({
    input: {
        width: '100%',
    },
    wrapper: {
        display: 'flex',
    },
    marginRight: {
        marginRight: '20px',
        flexShrink: 0
    }
});
