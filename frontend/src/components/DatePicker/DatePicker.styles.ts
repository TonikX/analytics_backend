import {createStyles, Theme} from "@mui/material";

export default (theme: Theme) => createStyles({
    marginBottom30: {
        marginBottom: '30px'
    },
    datePicker: {
        width: '100%',
    }
});