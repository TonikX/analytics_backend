import {createStyles, Theme} from "@material-ui/core";

export default (theme: Theme) => createStyles({
    label: {
        fontSize: '14px',
        marginBottom: 10
    },
    marginBottom30: {
        marginBottom: '30px'
    },
});